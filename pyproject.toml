[project]
name = "notes-migrator"
version = "0.1.0"
description = "Migrate notes from Apple Notes to Google Keep"
requires-python = ">=3.12"
dependencies = [
    "gkeepapi>=0.17.1",
]

[project.scripts]
migrate = "main:main"
